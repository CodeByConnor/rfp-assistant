[pytest]
testpaths = tests
# Silence one third-party deprecation (starlette's TestClient imports a
# renamed anyio alias). Targeted on purpose: warnings from this project's own
# code still show.
filterwarnings =
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
