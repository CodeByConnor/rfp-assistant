# Runtime dependencies.
#   pip install -r requirements.txt
#
# anthropic is only imported on the --live path; everything else in this
# project runs offline without it.

fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
openpyxl==3.1.5
pdfplumber==0.11.10
anthropic==1.5.0
